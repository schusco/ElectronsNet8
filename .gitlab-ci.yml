stages:
  - deploy
  - release

deploy_godaddy_dev:
  stage: deploy
  tags:
    - saas-windows-medium-amd64
  script:
    # Use the existing publish profile. 
    # Note: We pass the password via variable to keep it out of the repo.
    - dotnet publish Electrons.Net8/Electrons.Net8.csproj -c Release /p:PublishProfile="dev.electronsbaseball.com - Web Deploy.pubxml" /p:Password="$env:GD_PASSWORD" /p:AllowUntrustedCertificate=True
  only:
    - master

deploy_production:
  stage: deploy
  tags:
    - saas-windows-medium-amd64
  script:
    - dotnet publish Electrons.Net8/Electrons.Net8.csproj -c Release /p:PublishProfile="GoDaddy Electrons Prod - Web Deploy.pubxml" /p:Password="$env:GD_PASSWORD" /p:AllowUntrustedCertificate=True 
  when: manual
  only:
    - tags

create_release:
  stage: release
  tags:
    - saas-windows-medium-amd64
  script:
    - echo "Creating release for $CI_COMMIT_TAG"
  release:
    tag_name: '$CI_COMMIT_TAG'
    description: 'Stable build of ElectronsNet8 - $CI_COMMIT_TAG'
    ref: '$CI_COMMIT_SHA'
  only:
    - tags